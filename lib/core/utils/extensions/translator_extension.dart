import 'package:get/get.dart';

extension TranslatorExtension on String {
/*   String get tran {
    LocalzationController controller = Get.find();
    String data = this;
    if (controller.appLocals.isNotEmpty) {
      int? indexData = controller.appLocals.first.data!
          .indexWhere((element) => element.key == data);
      if (indexData! >= 0) {
        return controller.appLocals.first.data![indexData].value!;
      } else {
        return data.tr;
      }
    } else {
      return data.tr;
    }
  } */
}
